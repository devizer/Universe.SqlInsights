{
    "env": {
        "browser": true
    },
    "rules": {
        // Override our default settings just for this directory
        "unicode-bom": "off",
        "no-unused-vars": "warn"
        // "strict": "off"
    }
}
